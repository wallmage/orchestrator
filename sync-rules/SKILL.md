---
name: sync-rules
description: Sync master CLI rules ~/.codex/AGENTS.md to Grok, CodeBuddy, Cursor global rules files. Use on "/sync-rules" or "sync the rules".
---

Run `sh ~/.claude/skills/orchestrator/sync-rules.sh`. Report synced paths. Never touch `~/.claude/CLAUDE.md`.
